sudo: false
language: node_js
node_js:
  - '6'

notifications:
  email: false

cache:
  bundler: true
  directories:
  - node_modules

before_script:
  - npm install

script:
  - npm run test
  - npm run default
  - npm run deploy

after_success:
  - exit 0
